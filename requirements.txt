# Base runtime: everything needed to preprocess data and train a model, except
# PyTorch. Install that separately -- see requirements-torch.txt -- because the
# right wheel depends on whether the machine has CUDA.
#
#   pip install -r requirements.txt -r requirements-torch.txt
#
# Optional extras:
#   requirements-web.txt   the FastAPI job runner under webui/
#   requirements-dev.txt   test runner and tooling
#
# Versions are the ones this repository is tested against.
#
# This file used to be UTF-16 encoded, which pip cannot read at all, and it
# listed matplotlib and seaborn, which nothing imports since that research directory was
# removed, while omitting torch, numpy, scipy, rich, PyYAML and einops.

numpy==2.3.5
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.8.0

PyYAML==6.0.3
rich==14.2.0
typer==0.21.1
tqdm==4.67.1

einops==0.8.1
wandb==0.23.1
