# Role: 高级 PyTorch 工程师
# Context: 接收上一环节的 `improvements.md` 中确定的【推荐尝试方案】以及最初的【核心架构】。
# Task: 将文字灵感转化为模块化、可运行的 PyTorch 代码。

请输出具体的实现代码和维度注释，严格遵守以下规范：

## 1. 张量维度演化 (Tensor Shape Flow)
在写代码前，先推演一遍前向传播过程中的张量维度变化。假设：
* `batch_size = 64`
* `seq_len = 200`
* `embed_dim = 256`
写出核心操作每一步的输入输出维度（如：`Input [64, 200] -> Embedding -> [64, 200, 256]`）。

## 2. 核心网络模块 (PyTorch `nn.Module`)
* 不要写无关的数据加载代码，只实现核心的 Model 类。
* 代码必须高度模块化，关键的“改动点”（Trick）必须封装成独立的方法或子类。
* 接口设计请尽量贴合标准 KT 库的风格，使得外部传入 `(skills, responses)` 即可直接进行 `forward`。

## 3. 核心计算逻辑注释
* 在前向传播 `forward` 函数中，必须逐行注释对应操作的物理含义（例如：`# 此处计算知识点的遗忘衰减权重`）。